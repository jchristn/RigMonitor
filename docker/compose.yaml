services:
  rigmonitor:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: rigmonitor
    ports:
      - "9990:9990"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
